services:
  client:
    image: ghcr.io/xfcced/nilo-demo-client:latest
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - /etc/letsencrypt/live/nilo.luchang.xyz/fullchain.pem:/etc/nginx/certs/fullchain.pem:ro
      - /etc/letsencrypt/live/nilo.luchang.xyz/privkey.pem:/etc/nginx/certs/privkey.pem:ro
    restart: unless-stopped

  server:
    image: ghcr.io/xfcced/nilo-demo-server:latest
    environment:
      PORT: "4433"
      TLS_CERT_FILE: /certs/fullchain.pem
      TLS_KEY_FILE: /certs/privkey.pem
    volumes:
      - /etc/letsencrypt/live/wt.luchang.xyz/fullchain.pem:/certs/fullchain.pem:ro
      - /etc/letsencrypt/live/wt.luchang.xyz/privkey.pem:/certs/privkey.pem:ro
    ports:
      - "443:4433/udp"
    restart: unless-stopped
